[build]
buildCommand = "cd client && npm install && npm run build && cd ../server && npm install && npx prisma generate && npx tsc"

[deploy]
startCommand = "cd server && npx prisma migrate deploy && node dist/index.js"
